# Used only if you generate module migrations in this plugin project.
# This payment provider does not ship DB modules.
DB_USERNAME=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=medusa_plugin_dev

# For the local Medusa harness, copy and edit:
#   cp dev-store/.env.example dev-store/.env
# Then: npm run env:setup && npm run env:dev
